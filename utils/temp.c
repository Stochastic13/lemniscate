#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <mpfr.h>

int main(int argc, char** argv){
	for (int i = 0; i < argc - 1; i++){
//      Needs mpfr 4.1, removed for now
//		printf("%s\t%d\n", *(argv + 1 + i), mpfr_get_str_ndigits(10, atoi(*(argv + 1 + i))));
	}
}

